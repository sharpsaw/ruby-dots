#!/bin/sh -xe
## Spawn a shell in the dir of a gem.
gem=$1
if [ -e Gemfile ]; then
    cd $(bundle show $gem)
else
    cd `ruby -r$gem -e \
      "print File.expand_path \\$LOADED_FEATURES.grep(/$gem.rb/)[0] + '../'*3"`
fi
: Exit this shell to go back to where you were
$SHELL
